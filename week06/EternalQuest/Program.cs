using System;

class Program
{
    static void Main(string[] args)
    {
        //I imagine this is where the game would be if I created one. 
        //I only created the classes and methods to handle the goals of the game.
        //I did not create the game itself.
        GoalManager goalManager = new GoalManager();
        goalManager.Start();

    }
}